default-group: publish
groups:
  publish:
    generators:
      - name: fernapi/fern-typescript-node-sdk
        version: 0.46.0
        config:
          namespaceExport: WeatherGov
          noSerdeLayer: true
          outputSourceFiles: true
        output:
          location: local-file-system
          path: ../sdks/typescript
          # location: npm
          # package-name: weather-gov-sdk
          # token: ${NPM_TOKEN}
        
# api:
#   path: definition/__package__.yml
